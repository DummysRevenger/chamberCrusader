using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class boneWishOutcome : MonoBehaviour
{

    public static boneWishOutcome S;

    public string theOutcome;

    // Start is called before the first frame update
    void Start()
    {
        S = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
